server:
  port: 8081
spring:
  application:
    name: account-service

  config:
    import: "optional:configserver:http://localhost:8071"
    fail-fast: true

  management:
    endpoints:
      web:
        exposure:
          include: health,info,refresh

