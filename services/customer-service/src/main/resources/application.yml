server:
  port: 8082
spring:
  application:
    name: customer-service
  config:
    import: "optional:configserver:http://localhost:8071"
    fail-fast: true
  management:
    endpoints:
      web:
        exposure:
          include: health,info,refresh


